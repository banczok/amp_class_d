# Core service
fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.24
pyserial-asyncio>=0.6

# moOde talks MPD on 6600; this is how we ask it what is playing.
python-mpd2>=3.1

# Fingerprinting the analogue input.  Unofficial Shazam client - it works
# well and costs nothing, but it is not a supported API and can break.
shazamio>=0.6

# Cover art resizing: the knob needs exactly 480x480, the panel 400x400.
Pillow>=10.0
